# Quickstart: Running the Tests

## End-to-End Tests (Frontend)

To run the Playwright end-to-end tests for the frontend, use the following command from the `frontend` directory:

```bash
npm test
```

## Backend Tests

To run the pytest tests for the backend, use the following command from the `backend` directory:

```bash
pytest
```
